[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "scenario-engine"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["scenario_engine"]

[tool.setuptools.package-data]
scenario_engine = ["**/*.json", "**/*.md"]